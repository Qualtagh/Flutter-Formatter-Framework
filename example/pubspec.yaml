name: flutter_formatter_framework_example
description: Example project for Flutter Formatter Framework
publish_to: 'none'

environment:
  sdk: "^3.7.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_formatter_framework:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
